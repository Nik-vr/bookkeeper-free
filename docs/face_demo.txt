1. Объединить списки доходов и расходов, обозначив каждый тип записей значками (пока всего 4 вида: доход, расход, корзина-расход, фин.операция).
    а) структуру БД оставить неизменной (раздельные таблицы для доходов и расходов), чтобы упросить выборки за пределами общей сетки;
    б) добавить новый фильтр: "Только доходы", "Только расходы", "Доходы и расходы";
    в) при выборе в фильтре одной из категорий, визуальные значки по возможности скрывать;
    г) для правильного упорядочивания записей в единой сетке расходов и доходов вносить в БД полную дату добавления записи (без отбрасывания времени), но во всех случаях, кроме сортировки сетки использовать "округлённую дату" (только число/месяц/год), поскольку время внесения записи не несёт смысловой нагрузки (оно не связано с фактическим временем дохода/расхода);

2. Убрать разворачивание корзин:
    а) информацию о составе корзины показывать в виде всплывающей подсказки;
    б) редатирование записей обеспечить в окне редактирования корзины;

3. Убрать столбцы, не содержащие важной информации (упростить восприятие пользователем большого обьёяма информации). Для "зацепа" глаз достаточно "категории" (возможно, стоит показывать категорию и подкатегорию), суммы (без количества), примечания и даты.
    а) в общем случае не показывать дату для всех записей (число/месяц для первой записи в данный день, для прочих не выводить ничего; год только если в выборке имеются записи не за текущий год);
    б) при упорядочивании не по дате показывать число/месяц для всез записей (с годом ангалогично п. 3-а);

4. Добавить к строкам всплывающие подсказки с информацией, отсутствующей в таблице: цена, количество, состав корзины (см. п. 2.-а)
    а) возможно дублирование части информации (категория, полная дата, сумма);
    б) подсказки выводить при наведении мыши на запись, или при нажатии кнопки "инфо" в последнем столбце таблицы (последний вариант "тяжелее" в визуальном плане);

5. Кнопки добавления расхода/дохода со вкладки убрать (исключить дублирование элементов интерфейса с главной панелью)
    а) на главной панели добавить кнопку добавления корзины;
    б) группу кнопок "Добавить ..." на главной панели отделить от прочих кнопок пробелом;
    в) кнопку "Редактировать" полностью заменить двойным щелчком по записи и пунктом контекстного меню, вызываемого при щелчке ПКМ на записи;
    
